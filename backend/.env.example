# Copia este archivo a .env y rellena los valores

# PostgreSQL
DB_HOST=localhost
DB_PORT=5432
DB_NAME=lacteos_sofia
DB_USER=postgres
DB_PASSWORD=tu_password_aqui

# JWT — cambia esto por una cadena larga y aleatoria
JWT_SECRET=cambia_esto_por_un_secreto_muy_largo_y_seguro

# Puerto del servidor
PORT=3000

# Contraseña inicial del admin (solo se usa en el primer setup)
ADMIN_PASSWORD=sofia2024
